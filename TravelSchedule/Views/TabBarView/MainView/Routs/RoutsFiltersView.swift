import SwiftUI

struct RoutsFiltersView: View {
    @EnvironmentObject private var vM: MainVM
    
    var body: some View {
        VStack {
            
        }
    }
}

#Preview {
    RoutsFiltersView()
        .environmentObject(MainVM())
}
