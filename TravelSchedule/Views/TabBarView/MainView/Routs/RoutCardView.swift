import SwiftUI

struct RoutCardView: View {
    @EnvironmentObject private var vM: MainVM
    
    var body: some View {
        VStack {
            
        }
    }
}

#Preview {
    RoutCardView()
        .environmentObject(MainVM())
}
